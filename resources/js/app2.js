import './bootstrap';
const ul = document.getElementById('');
axios.get('endpoint');
array.forEach(element => {
    const li = document.createElement('li');
    const link = document.createElement('a');
    link.href = /*route of the page*/ 
    li.textContent(/*element of array gotten*/);
    link.append(li);
});